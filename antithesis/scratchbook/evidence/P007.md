# P007: atomic MCP service

Precondition: a valid case has an empty summons topic.

Attempt: submit a multi-value batch and fail one storage operation.

Assert: the topic contains either every value in order or no value.

Current evidence: `AppendBatch` contract and memory batch tests. Missing
evidence: Kafka commit ambiguity test for paperwork.
