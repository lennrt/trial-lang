# P002: restart without repetition

Precondition: the workload records each acknowledged step.

Attempt: replace the official at every commit boundary.

Assert: each acknowledged effect appears once and execution resumes after it.

Current evidence: `crashEverywhere` and fixed-seed generated programs.
