# P012: canonical case identifiers

Precondition: identifiers cover length, prefix, case, and hexadecimal edges.

Attempt: parse each identifier before any storage call.

Assert: only `case-` plus 24 lowercase hexadecimal digits succeeds.

Current evidence: `TestParseCaseRequiresCanonicalIdentifier` plus CLI and MCP
call paths.
