# P008: failed filing cleanup

Precondition: case topics are new.

Attempt: fail topic creation or the population batch, then fail cleanup.

Assert: successful cleanup removes the case. Failed cleanup returns both errors.

Current evidence: `TestFileCleansUpAfterPopulationFailure` and
`TestFileReportsCleanupFailure`. Broker-side partial topic-creation faults still
need an injected broker run.
