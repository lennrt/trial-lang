# P017: LSP bounds

Precondition: Counsel starts with no documents.

Attempt: send long headers, oversized payloads, oversized documents, and too
many documents.

Assert: input is rejected or diagnosed and does not enter document state.

Current evidence: `TestReadMessageRejectsOversizedPayload`,
`TestReadMessageRejectsOversizedHeader`, and
`TestDocumentStoreBoundsAndPreservesOldText`, and
`TestDocumentStoreBoundsOpenDocumentCount`.
