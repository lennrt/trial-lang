# P011: snapshot bounds

Precondition: records approach the configured count or byte limit.

Attempt: read at, below, and above each boundary.

Assert: allowed snapshots succeed. Larger snapshots return
`ErrResourceLimit` before an oversized result is allocated.

Current evidence: `TestResourceLimitBoundaries`. A live-broker recovery test
near the byte limit is still missing.
