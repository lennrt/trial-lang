# P010: fetch cancellation

Precondition: a fetch waits beyond the current topic end.

Attempt: cancel its context for 100 bounded repetitions.

Assert: each waiter returns `context.Canceled` before the test deadline.

Current evidence: `TestMemoryFetchCancellationDoesNotLeakWaiters`.
