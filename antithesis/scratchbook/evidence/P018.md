# P018: MCP bounds

Precondition: Advocate starts with a bounded log.

Attempt: send unknown fields, trailing JSON, negative offsets, large pages,
large sources, large batches, and verbose proceedings.

Assert: each invalid call fails closed and successful responses remain within
the configured output bounds.

Current evidence: `TestToolArgumentsFailClosed`,
`TestServeRejectsImpreciseBatchBeforeWriting`, `TestObservePaginates`, and
`TestToolSourceAndBatchBounds`, and `TestBoundedNarration`.
